{
  "name": "aws-docx-replacer",
  "version": "0.0.1",
  "description": "A module that grabs variable names and replaces with values using DOCX files from Amazon S3.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npm run lint && npm start",
    "lint": "eslint '**/*.js' --ignore-pattern 'node_modules/*'"
  },
  "author": "Marco Carmona",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "2.157.0",
    "docxtemplater": "3.1.12",
    "jszip": "2.6.1"
  },
  "devDependencies": {
    "eslint": "4.12.0"
  }
}
